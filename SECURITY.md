# 安全策略

## 报告安全问题

请勿在公开 Issue 中提交真实会员数据、DeepSeek 密钥、企业微信 Webhook、数据库密码或登录凭据。生产运营方应指定一个安全联系邮箱，并在上线前补充到此文件。

## 生产基线

- `.env` 仅存在于 ECS，权限建议设为 `600`。
- SSH 仅允许密钥登录，并在阿里云安全组限制管理来源 IP。
- PostgreSQL 不映射公网端口。
- 会员、教练、管理员使用不同账号；管理员密码不得复用。
- 每日备份，备份副本加密后同步到 OSS，并执行恢复演练。
- DeepSeek 与企业微信密钥定期轮换；Webhook 泄露后立即删除机器人并重新创建。
- 审计日志不得记录密码、Cookie、完整 API 密钥或完整 Webhook。
- 依赖和基础镜像按月更新，高危漏洞评估后及时修复。

## AI 与健康安全

Hermes 的输出不构成医疗诊断。重要建议必须经过教练确认；持续疼痛、夜间痛、眩晕、胸闷等情况必须提示停止训练并咨询合格医务人员。不得让模型直接执行高风险动作或绕过教练审批发送消息。
